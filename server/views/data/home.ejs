<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Prototype</title>
    <style>
        .login_buttons{
            visibility: hidden;
        }
        .test-button {
                margin-left: auto;
                margin-right: auto;
                margin-top: 10px;
                margin-bottom: 10px;
                border: 2px solid black;
                background-color: white;
                color: black;
                display: block;
                cursor: pointer;
            }

        .test-button:hover{
            background-color: green;
            color:white;
        }

        .test-danger-button:hover{
            background-color:red;
            color:white;
        }
    </style>
</head>
<body>
    <!-- <div class="container" id="loginform">
        <form action="/data/userlogin" method="POST">
            <div class="form-group">
                <label for="username"> Username: </label>
                <input required type="text" name = "username" id="username" class="form-control">
            </div>
            
            <div class="form-group">
                <label for="password"> Password: </label>
                <input required type="password" name = "password" id="password" class="form-control">
            </div>
    
            <a href="/data/registerForm" class="btn btn-secondary"> Register </a>
            <button type="submit" class="btn btn-primary"> Log In </button>
        </form>
        <%= "This is the username" + locals.username %>
        <br>
        <%= locals.username==null %>

        <% if(locals.username!='' && locals.username!="undefined" && locals.username){ %>
            <% if (locals.success){%>
                <p class="text-success"> <%= locals.success + " " + locals.username%></p>
            <% } %>
    
            <% if (locals.failure){%>
                <p class="text-danger"> <%= locals.error %></p>
            <% } %>
        <% } %>
    </div> -->

    <% if(locals.username!='' && locals.username!="undefined" && locals.username!=null && locals.username){ %>
        
        <script>
            //console.log(locals.username)
            document.getElementById("loginform").style.visibility="hidden"
            document.getElementById("loginTrue").style.visibility="visible"
        </script>

        <div class="container", id="loginTrue">
            <h1 class="mb-4"> Welcome to the prototype <%= locals.username %>  </h1>
            <!-- <a href="/login" class="btn btn-success"> Log in </a> -->
            <br>
            <a href="/data/new" class="btn-lg test-button btn btn-success"> Enter New Data</a>
            <br>
            <a href="/data/delete" class="btn-lg test-button btn btn-success"> Delete Data</a>
            <br>
            <a href="/allData" class="btn-lg test-button btn btn-success"> View All Data</a>
            <br>
            <a href="/data/allDoctor" class="btn-lg test-button btn btn-success"> Doctors</a>
            <br>
            <a href="/data/allMeds" class="btn-lg test-button btn btn-success"> Medication </a>
            <br>
            <a href="/data/allHealth" class="btn-lg test-button btn btn-success"> Health History / Diagnosis</a>    
            <br>            
            <form action="/logout?_method=DELETE" method="POST">
                <button class="btn-lg test-button test-danger-button btn btn-danger" type="submit"> Log Out </button>
            </form>
        </div>
    <% } %>
</body>
</html>